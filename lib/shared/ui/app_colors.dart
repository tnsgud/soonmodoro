import 'dart:ui';

/// 화면 배경.
const bgColor = Color(0xff141218);

/// 진행률 트랙, 비활성 인디케이터, 카드 배경.
const trackColor = Color(0xff2a2630);

/// 강조색. 진행률, 선택된 모드, 통계 수치.
const primaryColor = Color(0xff8e5cd9);

/// 타이머 모드 라벨.
const labelColor = Color(0xffc9c2d6);

/// 카드·버튼 표면.
const surfaceColor = Color(0x0Dffffff);

/// 카드·버튼 테두리.
const surfaceBorderColor = Color(0x1fffffff);

/// 보조 텍스트.
const mutedColor = Color(0xff8a8594);

/// 헤더 상단의 작은 라벨.
const mutedStrongColor = Color(0xff7a7f84);
